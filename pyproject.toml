[project]
name = "soundboard-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "imageio-ffmpeg>=0.6.0; sys_platform == 'win32'",
    "mutagen>=1.48.1",
    "pyside6>=6.11.1",
]
